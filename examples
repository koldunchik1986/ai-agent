# Примеры использования AI-агента
 
## 📋 Содержание
 
1. [Программирование](#-программирование)
2. [Юридические вопросы](#-юридические-вопросы)
3. [Работа с документами](#-работа-с-документами)
4. [Анализ кода](#-анализ-кода)
5. [Обучение модели](#-обучение-модели)
6. [Расширенные сценарии](#-расширенные-сценарии)
 
## 💻 Программирование
 
### Пример 1: Написание функции
 
```
🔍 Ваш запит: Напиши функцію на Python для перевірки, чи є число простим
 
🤖 Відповідь агента:
Ось функція для перевірки простих чисел з оптимізацією:
 
```python
import math
 
def is_prime(n):
    """Перевіряє, чи є число n простим."""
    if n <= 1:
        return False
    if n <= 3:
        return True
    if n % 2 == 0 or n % 3 == 0:
        return False
    
    # Перевіряємо дільники до кореня з n
    i = 5
    w = 2
    while i * i <= n:
        if n % i == 0:
            return False
        i += w
        w = 6 - w
    
    return True
 
# Приклад використання
for num in range(1, 21):
    if is_prime(num):
        print(f"{num} - просте число")
```
 
Оптимізації:
- Використання математичного підходу (перевірка до √n)
- Пропуск парних чисел після 2
- Патерн 6k ± 1 для перевірки дільників
```
 
### Пример 2: Рефакторинг кода
 
```
🔍 Ваш запит: Проаналізуй цей код і запропонуй рефакторинг:
```python
def calculate(x, y, z):
    a = x + y
    b = a * z
    c = b - x
    d = c / y
    return d
```
 
🤖 Відповідь агента:
Аналіз коду та пропозиції щодо рефакторингу:
 
**Проблеми поточного коду:**
1. Непрозорі імена змінних (a, b, c, d)
2. Відсутність обробки ділення на нуль
3. Складність розуміння логіки
4. Відсутність документації
 
**Покращена версія:**
```python
def calculate_weighted_difference(x: float, y: float, z: float) -> float:
    """
    Обчислює зважену різницю з трьох параметрів.
    
    Формула: ((x + y) * z - x) / y
    
    Args:
        x: Перший параметр
        y: Другий параметр (не може бути 0)
        z: Третій параметр (ваговий коефіцієнт)
    
    Returns:
        float: Результат обчислення
    
    Raises:
        ValueError: Якщо y дорівнює 0
    """
    if y == 0:
        raise ValueError("Параметр y не може дорівнювати 0")
    
    sum_xy = x + y
    weighted_sum = sum_xy * z
    difference = weighted_sum - x
    
    return difference / y
 
# Альтернативний варіант з більш описовими іменами
def calculate_adjusted_value(base: float, modifier: float, weight: float) -> float:
    """
    Розраховує скориговане значення на основі бази, модифікатора та ваги.
    """
    if modifier == 0:
        raise ValueError("Модифікатор не може бути нульовим")
    
    total = (base + modifier) * weight
    adjusted = total - base
    
    return adjusted / modifier
```
 
**Переваги рефакторингу:**
- Зрозумілі імена функцій та змінних
- Документація з прикладами
- Обробка помилок
- Типізація параметрів
- Очищена логіка розрахунків
```
 
### Пример 3: Дебаггинг
 
```
🔍 Ваш запит: У мене є баг у цьому коді, знайди його:
```python
def find_duplicates(items):
    duplicates = []
    for i in range(len(items)):
        for j in range(len(items)):
            if items[i] == items[j] and i != j:
                duplicates.append(items[i])
    return duplicates
```
 
🤖 Відповідь агента:
Знайдено кілька проблем у коді:
 
**Проблема 1: Дублікати в результаті**
Поточний код додає кожен дублікат двічі (для (i,j) та (j,i)).
 
**Проблема 2: Неефективність**
Часова складність O(n²) для кожної пари елементів.
 
**Проблема 3: Порядок дублікатів**
Результат містить дублікати в непередбачуваному порядку.
 
**Виправлена версія:**
```python
def find_duplicates(items):
    """Знаходить дублікати в списку з унікальними результатами."""
    seen = set()
    duplicates = set()
    
    for item in items:
        if item in seen:
            duplicates.add(item)
        else:
            seen.add(item)
    
    return list(duplicates)
 
# Альтернативний варіант з підрахунком
def find_duplicates_with_count(items):
    """Повертає дублікати з кількістю їх появ."""
    from collections import Counter
    
    counts = Counter(items)
    return {item: count for item, count in counts.items() if count > 1}
 
# Більш функціональний підхід
def find_duplicates_functional(items):
    """Функціональний підхід до пошуку дублікатів."""
    return list(set(item for item in items if items.count(item) > 1))
 
# Приклади використання
data = [1, 2, 3, 2, 4, 5, 3, 1]
print(find_duplicates(data))  # [1, 2, 3]
print(find_duplicates_with_count(data))  # {1: 2, 2: 2, 3: 2}
```
 
**Результати тестування:**
- Оригінальний код: [1, 2, 2, 3, 3, 1] (дублікати дублюються)
- Виправлений код: [1, 2, 3] (унікальні дублікати)
```
 
## ⚖️ Юридические вопросы
 
### Пример 1: Составление искового заявления
 
```
🔍 Ваш запит: Як скласти позовну заяву про стягнення заборгованості за договором позики?
 
🤖 Відповідь агента:
Для складання позовної заяви про стягнення заборгованості за договором позики необхідно дотримуватися вимог Цивільного процесуального кодексу України.
 
**Структура позовної заяви:**
 
```
Позовна заява
 
до ____________ суду
адреса: ____________________
 
Позивач: _________________
адреса: ___________________
телефон: _________________
email: ___________________
 
Відповідач: _______________
адреса: ___________________
телефон: _________________
 
Ціна позову: ____________ грн
 
ПОЗОВНА ЗАЯВА
про стягнення заборгованості за договором позики
```
 
**Опис обставин справи:**
```
[Дата] між позивачем та відповідачем було укладено договір позики, 
згідно з яким позивач надав відповідачу грошові кошти в сумі 
[сума] грн на умовах повернення до [дата] під [відсотки]% річних.
 
Договір було оформлено в письмовій формі та підписаний обома сторонами.
Відповідно до додатку №1 до позовної заяви, кошти були передані 
відповідачу [спосіб передачі].
 
Станом на поточну дату відповідач не виконав свої зобов'язання з повернення 
боргу, внаслідок чого сума заборгованості становить:
- Основний борг: [сума] грн
- Відсотки за користування: [сума] грн
- Інфляційні втрати: [сума] грн
Разом: [загальна сума] грн
```
 
**Правове обґрунтування:**
```
Відносини сторін регулюються:
- Цивільним кодексом України (статті 1046-1053)
- Законом України "Про забезпечення вимог кредиторів та реєстрацію обтяжень"
- Договором позики від [дата]
 
Відповідно до ст. 1046 ЦКУ за договором позики одна сторона (позикодавець) 
передає у власність другій стороні (позику) грошові кошти або інші речі, 
а позич зобов'язується повернути позикодавцю таку ж суму грошових коштів 
або таку ж кількість речей такого ж роду й якості.
 
Згідно зі ст. 1050 ЦКУ позич зобов'язаний повернути позику в 
строк, встановлений договором.
```
 
**Просильна частина:**
```
На підставі викладеного, керуючись ст. 1046-1053 Цивільного кодексу України,
статтями 161-164 Цивільного процесуального кодексу України,
 
ПРОШУ:
 
1. Стягнути з відповідача на користь позивача заборгованість за договором позики 
   в сумі [загальна сума] грн, в тому числі:
   - Основний борг: [сума] грн
   - Відсотки за користування: [сума] грн
 
2. Стягнути з відповідача судові витрати в сумі [сума] грн.
 
Додатки:
1. Копія договору позики
2. Копії документів, що підтверджують передачу коштів
3. Розрахунок заборгованості
4. Копія позовної заяви для відповідача
5. Документ про сплату судового збору
 
[Дата] _______________ (підпис)
```
 
**ВАЖЛИВО:** Ця відповідь надається в інформаційних цілях і не є юридичною консультацією. 
Для складання позовної заяви рекомендується звернутися до кваліфікованого юриста.
```
 
### Пример 2: Анализ законодательства
 
```
🔍 Ваш запит: Які права та обов'язки виникають при укладенні договору оренди житла згідно з законом України?
 
🤖 Відповідь агента:
Договір оренди житла в Україні регулюється Цивільним кодексом України, 
Житловим кодексом України та Законом України "Про оренду державного та комунального майна".
 
**Основні права орендаря (ст. 797-811 ЦКУ):**
 
1. **Право користування житлом:**
   - Використовувати житло для проживання
   - Розміщувати членів сім'ї
   - Зберігати особисті речі
 
2. **Право на належний стан житла:**
   - Вимагати від орендодавця належного стану житла
   - Вимагати проведення ремонту (якщо це обов'язок орендодавця)
 
3. **Право на конфіденційність:**
   - Недопустимость втручання орендодавця в приватне життя
   - Обмеження відвідувань без згоди орендаря
 
4. **Право на розірвання договору:**
   - За згодою сторін
   - В односторонньому порядку (за певних умов)
 
**Основні обов'язки орендаря:**
 
1. **Своєчасна оплата:**
   - Вносити орендну плату в встановлений термін
   - Сплачувати комунальні послуги (якщо вказано в договорі)
 
2. **Належне користування:**
   - Використовувати житло за призначенням
   - Не допускати псування майна
   - Повідомляти орендодавця про пошкодження
 
3. **Прийняття житла:**
   - Підписати акт приймання-передачі
   - Перевірити стан житла на момент отримання
 
**Обов'язки орендодавця:**
 
1. **Передача житла:**
   - Передати житло в стані, придатному для проживання
   - Забезпечити доступ до комунальних послуг
 
2. **Гарантії:**
   - Не заважати користуванню житлом
   - Проводити капітальний ремонт (якщо це його обов'язок)
 
**Важливі юридичні аспекти:**
 
**Реєстрація договору:**
- Договір оренди житла на строк понад 1 рік підлягає нотаріальному посвідченню
- Договір на строк понад 3 роки підлягає державній реєстрації
 
**Підстави для розірвання:**
- Систематичне порушення умов договору орендарем
- Знищення або пошкодження житла
- Невикористання житла протягом 6 місяців без поважних причин
 
**Рекомендована структура договору:**
1. Реквізити сторін
2. Предмет договору (характеристики житла)
3. Строк оренди
4. Орендна плата та порядок розрахунків
5. Права та обов'язки сторін
6. Відповідальність сторін
7. Порядок розірвання договору
8. Вирішення спорів
9. Реквізити та підписи
 
**Ризики та як їх уникнути:**
- Завжди укладайте договір в письмовій формі
- Зробіть фотофіксацію стану житла на момент передачі
- Перевірте право власності орендодавця
- Задокументуйте всі комунікації
```
 
## 📚 Работа с документами
 
### Пример 1: Добавление и поиск документов
 
```bash
# Запуск CLI
./scripts/run.sh cli
 
# Добавление документов
🔍 Ваш запит: add ~/documents/legal
 
🤖 Відповідь агента:
📁 Додавання документів...
✓ Документи додано успішно: 15 файлів оброблено
   - PDF документи: 8
   - DOCX документи: 5
   - TXT документи: 2
   - Створено ембеддинги: ✓
   - Додано до Knowledge Graph: 12
 
# Поиск по документам
🔍 Ваш запит: search договір оренди житла
 
🤖 Відповідь агента:
Знайдено 5 результатів:
 
1. [cyan]Договір_оренди_житла_2023.docx[/cyan] (релевантність: 0.95)
   [dim]Цей документ містить типову форму договору оренди житла з усіма необхідними розділами...[/dim]
 
2. [cyan]Закон_про_оренду.pdf[/cyan] (релевантність: 0.87)
   [dim]Закон України "Про оренду державного та комунального майна" з останніми змінами...[/dim]
 
3. [cyan]Позов_орендарний_спір.docx[/cyan] (релевантність: 0.82)
   [dim]Позовна заява щодо стягнення заборгованості за орендою житлового приміщення...[/dim]
```
 
### Пример 2: Обучение на документах
 
```bash
# Обучение модели
🔍 Ваш запит: train
 
🤖 Відповідь агента:
🎮 Запуск навчання моделі...
⏳ Підготовка даних: 15 документів, 234 чанки
📚 Створення навчальних прикладів: 1125 прикладів
🏋️ Навчання моделі: 3 епохи, batch_size=2
 
Епоха 1/3: loss=2.456, accuracy=0.67
Епоха 2/3: loss=1.234, accuracy=0.78  
Епоха 3/3: loss=0.892, accuracy=0.84
 
✓ Навчання завершено!
💾 Модель збережена: /home/sda3/ai-agent/models/fine_tuned_model_20240315_143022
📊 Метрики навчання:
   - Train loss: 0.892
   - Validation loss: 0.934
   - Training time: 2h 34m
   - Samples/sec: 3.45
```
 
## 🔍 Анализ кода
 
### Пример 1: Обнаружение уязвимостей
 
```
🔍 Ваш запит: Проаналізуй цей код на наявність вразливостей:
```python
import sqlite3
 
def login(username, password):
    conn = sqlite3.connect('users.db')
    cursor = conn.cursor()
    
    query = f"SELECT * FROM users WHERE username='{username}' AND password='{password}'"
    cursor.execute(query)
    
    user = cursor.fetchone()
    conn.close()
    
    return user is not None
```
 
🤖 Відповідь агента:
🚨 **ЗНАЙДЕНО КРИТИЧНІ ВУЗЛИВІСТІ!**
 
**1. SQL Injection (Критично)**
```python
# Вразливий код
query = f"SELECT * FROM users WHERE username='{username}' AND password='{password}'"
```
 
**Атака:** `username=admin'--` обійде перевірку пароля.
**Ризик:** Повний доступ до бази даних.
 
**Виправлення:**
```python
def login(username, password):
    conn = sqlite3.connect('users.db')
    cursor = conn.cursor()
    
    # Безпечний варіант з параметризованими запитами
    query = "SELECT * FROM users WHERE username = ? AND password = ?"
    cursor.execute(query, (username, password))
    
    user = cursor.fetchone()
    conn.close()
    
    return user is not None
```
 
**2. Небезпечне зберігання паролів (Високо)**
- Паролі зберігаються у відкритому вигляді
- Відсутність хешування
 
**Виправлення:**
```python
import hashlib
import sqlite3
import secrets
 
def hash_password(password, salt=None):
    if salt is None:
        salt = secrets.token_hex(16)
    
    password_hash = hashlib.pbkdf2_hmac('sha256', 
                                      password.encode('utf-8'), 
                                      salt.encode('utf-8'), 
                                      100000)
    return salt + password_hash.hex()
 
def verify_password(password, hashed_password):
    salt = hashed_password[:32]
    stored_hash = hashed_password[32:]
    return hash_password(password, salt) == hashed_password
 
def create_user(username, password):
    conn = sqlite3.connect('users.db')
    cursor = conn.cursor()
    
    hashed_pw = hash_password(password)
    cursor.execute("INSERT INTO users (username, password) VALUES (?, ?)", 
                  (username, hashed_pw))
    
    conn.commit()
    conn.close()
 
def login(username, password):
    conn = sqlite3.connect('users.db')
    cursor = conn.cursor()
    
    cursor.execute("SELECT password FROM users WHERE username = ?", (username,))
    result = cursor.fetchone()
    conn.close()
    
    if result and verify_password(password, result[0]):
        return True
    return False
```
 
**3. Додаткові рекомендації:**
- Використовуйте SQLAlchemy для ORM
- Додайте rate limiting для запобігання brute-force атак
- Використовуйте HTTPS для передачі даних
- Реалізуйте 2FA для додаткової безпеки
```
 
### Пример 2: Оптимізація продуктивності
 
```
🔍 Ваш запит: Оптимізуй цей код для кращої продуктивності:
```python
def process_large_file(filename):
    with open(filename, 'r') as f:
        lines = f.readlines()
    
    processed = []
    for line in lines:
        if line.strip():
            words = line.split()
            filtered = [w for w in words if len(w) > 3]
            processed.extend(filtered)
    
    return processed
 
def find_duplicates(items):
    duplicates = []
    for i in range(len(items)):
        for j in range(i + 1, len(items)):
            if items[i] == items[j] and items[i] not in duplicates:
                duplicates.append(items[i])
    return duplicates
```
 
🤖 Відповідь агента:
📊 **Аналіз продуктивності та оптимізація:**
 
**Проблема 1: Завантаження всього файлу в пам'ять**
```python
# Неефективно
lines = f.readlines()  # Завантажує весь файл
```
 
**Проблема 2: Високий час виконання O(n²)**
```python
# Неефективний пошук дублікатів
for i in range(len(items)):
    for j in range(i + 1, len(items)):
```
 
**Оптимізована версія:**
```python
from typing import Iterator, List, Set
import re
from collections import defaultdict
 
def process_large_file_optimized(filename: str) -> List[str]:
    """
    Оптимізована обробка великих файлів з мінімальним використанням пам'яті.
    """
    processed = []
    
    with open(filename, 'r', encoding='utf-8') as f:
        for line in f:  # Ітератор, не завантажує весь файл
            line = line.strip()
            if not line:
                continue
            
            # Використовуємо regex для кращої продуктивності
            words = re.findall(r'\b\w{4,}\b', line.lower())
            processed.extend(words)
    
    return processed
 
def find_duplicates_optimized(items: List[str]) -> List[str]:
    """
    Оптимізований пошук дублікатів з використанням set O(n).
    """
    seen = set()
    duplicates = set()
    
    for item in items:
        if item in seen:
            duplicates.add(item)
        else:
            seen.add(item)
    
    return list(duplicates)
 
def find_duplicates_with_count(items: List[str]) -> dict:
    """
    Повертає дублікати з кількістю появ.
    """
    from collections import Counter
    
    counts = Counter(items)
    return {item: count for item, count in counts.items() if count > 1}
 
def batch_process_files(file_paths: List[str]) -> dict:
    """
    Батчева обробка файлів для максимальної продуктивності.
    """
    results = {}
    
    for file_path in file_paths:
        try:
            processed_words = process_large_file_optimized(file_path)
            duplicates = find_duplicates_optimized(processed_words)
            results[file_path] = {
                'total_words': len(processed_words),
                'duplicates': len(duplicates),
                'unique_words': len(set(processed_words)),
                'duplicate_words': duplicates[:10]  # Перші 10 дублікатів
            }
        except Exception as e:
            results[file_path] = {'error': str(e)}
    
    return results
 
# Паралельна обробка для багатоядерних систем
from concurrent.futures import ThreadPoolExecutor
import multiprocessing
 
def parallel_process_files(file_paths: List[str]) -> dict:
    """
    Паралельна обробка файлів з використанням всіх ядер CPU.
    """
    max_workers = min(multiprocessing.cpu_count(), len(file_paths))
    
    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        future_to_file = {
            executor.submit(process_large_file_optimized, file_path): file_path 
            for file_path in file_paths
        }
        
        results = {}
        for future in concurrent.futures.as_completed(future_to_file):
            file_path = future_to_file[future]
            try:
                processed_words = future.result()
                results[file_path] = {
                    'total_words': len(processed_words),
                    'duplicates': len(find_duplicates_optimized(processed_words))
                }
            except Exception as e:
                results[file_path] = {'error': str(e)}
    
    return results
```
 
**Результати оптимізації:**
- **Використання пам'яті**: Зменшено на 90% (читання по рядках)
- **Швидкість пошуку дублікатів**: З O(n²) до O(n)
- **Паралельна обробка**: Прискорення до 8x на 8-ядерному CPU
- **Типізація**: Краща підтримка IDE та перевірка типів
```
 
## 🎓 Обучение модели
 
### Пример 1: Подготовка датасета
 
```python
# Створення датасету для навчання
from document_processor import create_document_processor
from model_training import create_trainer
 
def prepare_training_data():
    """Підготовка даних для навчання."""
    
    # Ініціалізація процесора документів
    processor = create_document_processor()
    
    # Обробка документів
    documents = processor.process_directory("/home/sda3/ai-agent/documents")
    
    print(f"Оброблено документів: {len(documents)}")
    
    # Створення тренера
    trainer = create_trainer()
    
    # Підготовка навчальних даних
    train_dataset, eval_dataset = trainer.prepare_training_data(documents)
    
    print(f"Навчальних прикладів: {len(train_dataset)}")
    print(f"Валідаційних прикладів: {len(eval_dataset)}")
    
    return trainer, train_dataset, eval_dataset
 
# Запуск навчання
trainer, train_data, eval_data = prepare_training_data()
 
# Обучение с сохранением
result = trainer.train_model(
    documents, 
    save_path="/home/sda3/ai-agent/models/specialized_model"
)
```
 
### Пример 2: Специализированное обучение
 
```python
# Навчання на юридичних документах
def train_legal_model():
    """Спеціалізоване навчання на юридичних документах."""
    
    # Фільтрація юридичних документів
    legal_documents = [
        doc for doc in documents 
        if doc.document_type == "legal"
    ]
    
    if not legal_documents:
        print("Юридичні документи не знайдено")
        return
    
    # Налаштування параметрів для юридичних текстів
    config.training.learning_rate = 1e-5  # Нижча швидкість навчання
    config.training.num_train_epochs = 5   # Більше епох
    config.training.lora_r = 32            # Більший ранг LoRA
    
    # Створення тренера
    trainer = create_trainer()
    
    # Запуск навчання
    result = trainer.train_model(
        legal_documents,
        save_path="/home/sda3/ai-agent/models/legal_specialist"
    )
    
    print("✅ Юридична модель навчена успішно!")
    return result
 
# Навчання на програмних документах
def train_programming_model():
    """Спеціалізоване навчання на програмному коді."""
    
    programming_documents = [
        doc for doc in documents 
        if doc.document_type == "programming"
    ]
    
    if not programming_documents:
        print("Програмні документи не знайдено")
        return
    
    # Налаштування для коду
    config.training.learning_rate = 5e-5   # Вища швидкість навчання
    config.training.num_train_epochs = 3   # Менше епох
    config.training.temperature = 0.3      # Більш детерміновані відповіді
    
    trainer = create_trainer()
    result = trainer.train_model(
        programming_documents,
        save_path="/home/sda3/ai-agent/models/code_specialist"
    )
    
    print("✅ Програмна модель навчена успішно!")
    return result
```
 
## 🚀 Расширенные сценарии
 
### Пример 1: Мультимодальный агент
 
```python
# Розширення для роботи з зображеннями та аудіо
class MultimodalAgent(AIAgent):
    """Мультимодальний агент з підтримкою зображень та аудіо."""
    
    def __init__(self):
        super().__init__()
        self.image_processor = None
        self.audio_processor = None
        
    def process_image_query(self, image_path: str, query: str) -> AgentResponse:
        """Обробка запиту з зображенням."""
        try:
            # Обробка зображення
            image_features = self.extract_image_features(image_path)
            
            # Пошук схожих документів
            context = self.search_by_image_features(image_features)
            
            # Генерація відповіді
            return self.generate_response_with_image_context(query, context, image_path)
            
        except Exception as e:
            logger.error(f"Error processing image query: {e}")
            return AgentResponse(
                content=f"Помилка обробки зображення: {e}",
                sources=[],
                confidence=0.0,
                response_time=0.0,
                context_used=False
            )
    
    def extract_image_features(self, image_path: str):
        """Витягнення ознак з зображення."""
        # Реалізація з використанням CLIP або іншої моделі
        pass
    
    def process_audio_query(self, audio_path: str, query: str) -> AgentResponse:
        """Обробка запиту з аудіо."""
        try:
            # Транскрипція аудіо
            transcript = self.transcribe_audio(audio_path)
            
            # Об'єднання з текстовим запитом
            combined_query = f"{query}\
\
Транскрипція аудіо: {transcript}"
            
            # Стандартна обробка
            return self.query(combined_query)
            
        except Exception as e:
            logger.error(f"Error processing audio query: {e}")
            return AgentResponse(
                content=f"Помилка обробки аудіо: {e}",
                sources=[],
                confidence=0.0,
                response_time=0.0,
                context_used=False
            )
```
 
### Пример 2: API сервер
 
```python
# Створення FastAPI сервера
from fastapi import FastAPI, UploadFile, File, HTTPException
from fastapi.responses import JSONResponse
import uvicorn
 
app = FastAPI(title="AI Agent API", version="1.0.0")
 
agent = create_ai_agent()
 
@app.post("/query")
async def query_agent(request: dict):
    """Обробка текстового запиту."""
    try:
        query = request.get("query", "")
        use_context = request.get("use_context", True)
        
        response = agent.query(query)
        
        return {
            "content": response.content,
            "sources": response.sources,
            "confidence": response.confidence,
            "response_time": response.response_time,
            "context_used": response.context_used
        }
        
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
 
@app.post("/upload_documents")
async def upload_documents(files: List[UploadFile] = File(...)):
    """Завантаження документів."""
    try:
        uploaded_files = []
        
        for file in files:
            # Збереження файлу
            file_path = f"/home/sda3/ai-agent/documents/{file.filename}"
            with open(file_path, "wb") as buffer:
                content = await file.read()
                buffer.write(content)
            
            uploaded_files.append(file_path)
        
        # Додавання до агента
        result = agent.add_documents(uploaded_files)
        
        return result
        
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
 
@app.get("/status")
async def get_status():
    """Отримання статусу агента."""
    return agent.get_agent_status()
 
@app.post("/train")
async def train_model():
    """Запуск навчання моделі."""
    try:
        result = agent.train_on_documents()
        return result
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
 
if __name__ == "__main__":
    uvicorn.run(app, host="0.0.0.0", port=8000)
```
 
Ці приклади демонструють широкий спектр можливостей AI-агента та можуть бути адаптовані під конкретні потреби користувачів.
 