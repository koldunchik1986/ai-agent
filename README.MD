# AI-агент на базе Mistral AI 7B Instruct v0.3
 
Полнофункциональный AI-агент с возможностью обучения на документах, специализирующийся на программировании и юридических вопросах Украины.
 
## 🌟 Возможности
 
- 🤖 **Основа**: Mistral AI 7B Instruct v0.3
- 📚 **Обучение**: Дообучение на PDF, DOC, DOCX документах
- 💻 **Программирование**: Vibe coding, анализ кода, отладка
- ⚖️ **Юридическая поддержка**: Законодательство Украины, составление исков
- 🕸️ **Knowledge Graph**: Neo4j для структурированных знаний
- 🔍 **RAG**: Retrieval Augmented Generation с векторной базой данных
- 🐳 **Docker**: Полная контейнеризация с GPU поддержкой
- 💬 **CLI**: Интерактивный интерфейс с богатым функционалом
 
## 🏗️ Архитектура
 
```
ai-agent/
├── src/                          # Исходный код
│   ├── config.py                 # Конфигурация системы
│   ├── document_processor.py     # Обработка документов
│   ├── model_training.py         # Обучение модели
│   ├── knowledge_graph.py        # Knowledge Graph (Neo4j)
│   ├── agent.py                  # Основной AI-агент
│   └── cli_interface.py          # CLI интерфейс
├── data/                         # Данные
│   ├── models/                   # Хранилище моделей
│   ├── documents/                # Обучающие документы
│   └── cache/                    # Кэш
├── scripts/                      # Скрипты
│   ├── setup.sh                  # Установка
│   └── run.sh                    # Управление
├── docs/                         # Документация
├── Dockerfile                    # Docker конфигурация
├── docker-compose.yml            # Сервисы
└── requirements.txt              # Зависимости
```
 
## 🚀 Быстрый старт
 
### 1. Системные требования
 
- **ОС**: Kali Linux (или Ubuntu 20.04+)
- **GPU**: NVIDIA P104-100 8GB (или другая с 8GB+ памятью)
- **CUDA**: 11.8+
- **RAM**: 16GB+ (рекомендуется 32GB)
- **Storage**: 50GB+ на /home
 
### 2. Установка
 
```bash
# Клонирование проекта
git clone https://github.com/koldunchik1986/ai-agent/
cd ai-agent
 
# Запуск установщика
sudo ./scripts/setup.sh
 
# Перезагрузка для применения изменений
sudo reboot
```
 
### 3. Первый запуск
 
```bash
# Запуск всех сервисов
./scripts/run.sh start
 
# Проверка здоровья системы
./scripts/run.sh health
 
# Запуск CLI интерфейса
./scripts/run.sh cli
```
 
## 📋 Подробная инструкция установки
 
### Шаг 1: Подготовка системы
 
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y
 
# Установка NVIDIA драйверов (если не установлены)
sudo apt install nvidia-driver-470 nvidia-cuda-toolkit
 
# Проверка GPU
nvidia-smi
```
 
### Шаг 2: Настройка путей
 
Создайте директории на диске:
 
```bash
sudo mkdir -p /home/ai-agent/{models,documents,cache,logs}
sudo chown -R $USER:$USER /home/ai-agent
```
 
### Шаг 3: Запуск установщика
 
```bash
# Запуск с правами root
sudo ./scripts/setup.sh
```
 
Установщик автоматически:
- Установит Docker и Docker Compose
- Настроит поддержку GPU
- Создаст необходимые директории
- Установит системные лимиты
- Настроит переменные окружения
 
### Шаг 4: Проверка установки
 
```bash
# Проверка здоровья системы
./scripts/run.sh health
 
# Просмотр логов
./scripts/run.sh logs
```
 
## 🎮 Использование
 
### CLI интерфейс
 
Основной способ взаимодействия с агентом - через CLI интерфейс:
 
```bash
# Запуск CLI
./scripts/run.sh cli
 
# или в режиме разработки
./scripts/run.sh dev
```
 
### Основные команды CLI
 
```
help                 - Показать справку
status               - Статус агента и системы
clear                - Очистить экран
history              - История команд
add <путь>          - Добавить документы
list-docs           - Показать документы
search <запрос>     - Поиск по документам
train               - Обучить модель
save-model <имя>    - Сохранить модель
set-temp <значение> - Установить температуру
set-tokens <число>  - Установить макс. токенов
exit/quit           - Выход
```
 
### Примеры использования
 
#### Программирование
 
```
🔍 Ваш запит: Напиши функцію для сортування масиву на Python
 
🤖 Відповідь агента:
```python
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    
    return quick_sort(left) + middle + quick_sort(right)
 
# Приклад використання
numbers = [3, 6, 8, 10, 1, 2, 1]
sorted_numbers = quick_sort(numbers)
print(sorted_numbers)  # [1, 1, 2, 3, 6, 8, 10]
```
 
Ця реалізація використовує алгоритм швидкого сортування з часовою складністю O(n log n)...
```
 
#### Юридические вопросы
 
```
🔍 Ваш запит: Як скласти позовну заяву про стягнення боргу?
 
🤖 Відповідь агента:
Для складання позовної заяви про стягнення боргу необхідно дотримуватися вимог Цивільного процесуального кодексу України.
 
Структура позовної заяви:
1. Шапка (назва суду, дані сторін)
2. Назва документа ("Позовна заява")
3. Опис обставин справи
4. Правове обґрунтування
5. Вимоги позивача
6. Перелік доказів
7. Дата та підпис
 
ВАЖЛИВО: Ця відповідь надається в інформаційних цілях і не є юридичною консультацією...
```
 
### Работа с документами
 
#### Добавление документов
 
```bash
# Добавление одного документа
./scripts/run.sh add ~/document.pdf
 
# Добавление директории с документами
./scripts/run.sh add ~/documents/
 
# Через CLI
add /path/to/documents
```
 
#### Поиск по документам
 
```bash
# Через CLI
search "ключові слова для пошуку"
 
# Агент найдет релевантные документы и покажет контент
```
 
#### Обучение модели
 
```bash
# Обучение на добавленных документах
train
 
# Сохранение обученной модели
save-model my-custom-model
```
 
## 🔧 Конфигурация
 
### Основные настройки (config.py)
 
```python
# Модель
model_name = "mistralai/Mistral-7B-Instruct-v0.3"
max_new_tokens = 2048
temperature = 0.7
 
# GPU
load_in_8bit = True  # Для 8GB GPU
device = "cuda"
 
# Пути
model_cache_dir = "/home/ai-agent/models"
documents_path = "/home/ai-agent/documents"
```
 
### Neo4j Knowledge Graph
 
- **URL**: bolt://localhost:7687
- **Пользователь**: neo4j
- **Пароль**: password
- **Web интерфейс**: http://localhost:7474
 
### Chroma Vector Store
 
- **URL**: http://localhost:8001
- **Коллекция**: ai-agent-collection
 
## 🛠️ Разработка
 
### Локальная разработка
 
```bash
# Активация виртуального окружения
source venv/bin/activate
 
# Установка зависимостей
pip install -r requirements.txt
 
# Запуск в режиме разработки
export AGENT_HOME="/home/ai-agent"
python -m src.cli_interface
```
 
### Структура кода
 
- **config.py**: Конфигурация системы и переменные окружения
- **document_processor.py**: Обработка PDF/DOC/DOCX, создание эмбеддингов
- **model_training.py**: LoRA обучение, квантизация, сохранение моделей
- **knowledge_graph.py**: Neo4j интеграция, извлечение сущностей
- **agent.py**: Основной агент, RAG, генерация ответов
- **cli_interface.py**: Rich CLI интерфейс с историей команд
 
### Добавление новых возможностей
 
1. **Новый тип документа**: Расширить `document_processor.py`
2. **Новая модель**: Изменить `config.py` и `model_training.py`
3. **Новый источник знаний**: Интегрировать в `knowledge_graph.py`
 
## 📊 Производительность
 
### Оптимизация для 8GB GPU
 
- 8-bit квантизация (bitsandbytes)
- LoRA обучение (параметр-эффективное)
- Gradient accumulation
- Mixed precision training
 
### Требования к памяти
 
- **Модель**: ~8GB GPU (8-bit)
- **Хост RAM**: 16GB+ (рекомендуется 32GB)
- **Storage**: 50GB+ для моделей и данных
 
### Скорость вывода
 
- **Без кэша**: ~5-10 токенов/сек
- **С контекстом**: ~3-7 токенов/сек
- **Обучение**: ~2-4 часов на 1000 документов
 
## 🔍 Мониторинг и отладка
 
### Проверка здоровья системы
 
```bash
./scripts/run.sh health
```
 
Проверяет:
- Статус Docker сервисов
- Доступность GPU
- Свободное место на диске
- Доступность web интерфейсов
 
### Логирование
 
```bash
# Просмотр логов всех сервисов
./scripts/run.sh logs
 
# Логи конкретного сервиса
docker-compose logs -f ai-agent
docker-compose logs -f neo4j
docker-compose logs -f chroma
```
 
### Метрики производительности
 
```python
# В CLI интерфейсе
status
```
 
Показывает:
- Загрузку модели
- Количество обработанных документов
- Статистику knowledge graph
- Память GPU
 
## 🗂️ Управление данными
 
### Резервное копирование
 
```bash
# Создание backup
./scripts/run.sh backup
 
# Восстановление из backup
./scripts/run.sh restore /path/to/backup
```
 
### Очистка кэша
 
```bash
# Очистка векторного хранилища
docker-compose exec chroma python -c "import chromadb; chromadb.PersistentClient('/chroma/chroma').delete_collection('ai-agent-collection')"
 
# Очистка knowledge graph
docker-compose exec neo4j cypher-shell -u neo4j -p password "MATCH (n) DETACH DELETE n"
```
 
## 🐛 Troubleshooting
 
### Проблемы с GPU
 
**Ошибка**: CUDA out of memory
```bash
# Решение: Увеличить swap файл
sudo fallocate -l 8G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```
 
**Ошибка**: GPU not detected
```bash
# Проверка драйверов
nvidia-smi
 
# Переустановка драйверов
sudo apt install nvidia-driver-470
sudo reboot
```
 
### Проблемы с Docker
 
**Ошибка**: Permission denied
```bash
# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```
 
**Ошибка**: Port already in use
```bash
# Проверка портов
netstat -tulpn | grep :7474
netstat -tulpn | grep :8001
 
# Остановка конфликтующих сервисов
sudo systemctl stop some-service
```
 
### Проблемы с моделью
 
**Ошибка**: Model not found
```bash
# Обновление модели
./scripts/run.sh update
 
# Ручная загрузка
docker-compose exec ai-agent python -c "
from transformers import AutoModelForCausalLM
AutoModelForCausalLM.from_pretrained('mistralai/Mistral-7B-Instruct-v0.3', cache_dir='/home/ai-agent/models')
"
```
 
**Ошибка**: Slow inference
```bash
# Проверка загрузки GPU
nvidia-smi
 
# Оптимизация конфигурации
# Уменьшить max_new_tokens
# Увеличить temperature для более быстрых ответов
```
 
## 📚 Дополнительные ресурсы
 
### Документация
 
- [Mistral AI Documentation](https://docs.mistral.ai/)
- [Transformers Library](https://huggingface.co/docs/transformers/)
- [LangChain Documentation](https://python.langchain.com/)
- [Neo4j Documentation](https://neo4j.com/docs/)
- [ChromaDB Documentation](https://docs.trychroma.com/)
 
### Модели и датасеты
 
- [Mistral 7B Models](https://huggingface.co/mistralai)
- [Ukrainian NLP Resources](https://lang.org.ua/)
- [Legal Datasets](https://huggingface.co/datasets)
 
### Сообщество
 
- [GitHub Issues](https://github.com/your-repo/issues)
- [Discord Community](https://discord.gg/your-server)
- [Stack Overflow](https://stackoverflow.com/questions/tagged/mistral-ai)
 
## 📄 Лицензия
 
Этот проект распространяется под MIT License. См. файл LICENSE для подробностей.
 
## 🤝 Вклад
 
Вклады приветствуются! Пожалуйста, создайте Pull Request с описанием изменений.
 
### Руководство по вкладу
 
1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request
 
## 📞 Поддержка
 
Если у вас возникли вопросы или проблемы:
 
1. Проверьте раздел [Troubleshooting](#-troubleshooting)
2. Поищите в [Issues](https://github.com/your-repo/issues)
3. Создайте новый Issue с подробным описанием
4. Свяжитесь с командой разработки
 
---
 
**Спасибо за использование AI-агента на базе Mistral AI 7B!** 🚀