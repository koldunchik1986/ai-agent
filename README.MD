# ===================================================================
# ПРИМЕР ПЕРЕМЕННЫХ ОКРУЖЕНИЯ ДЛЯ AI-АССИСТЕНТА
# ===================================================================
# Скопируйте этот файл в .env и отредактируйте под свои нужды
# Большинство параметров уже оптимизированы для P104-100 8GB VRAM

# ---------------------------------------------------------------
# ОСНОВНЫЕ ПУТИ
# ---------------------------------------------------------------
# Директория с данными проекта
AGENT_HOME=/app/data

# Поддиректории (не меняйте без необходимости)
MODEL_CACHE_PATH=/app/data/models
DOCUMENT_PATH=/app/data/documents
CACHE_PATH=/app/data/cache
VSCODE_PROJECTS_PATH=/app/data/vscode_projects

# ---------------------------------------------------------------
# GPU НАСТРОЙКИ (КРИТИЧНО ДЛЯ 8GB VRAM)
# ---------------------------------------------------------------
# CUDA устройство (0 для первой GPU)
CUDA_VISIBLE_DEVICES=0

# Оптимизации PyTorch allocator (экономия памяти)
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:128,garbage_collection_threshold:0.6

# Ограничение потоков (экономия RAM)
OMP_NUM_THREADS=1

# Отключение параллелизма в tokenizers (стабильность)
TOKENIZERS_PARALLELISM=false

# ---------------------------------------------------------------
# НАСТРОЙКИ МОДЕЛИ (МОЖНО ИЗМЕНИТЬ ПОД СВОИ ЗАДАЧИ)
# ---------------------------------------------------------------
# Температура генерации (0.3=точный, 0.7=креативный)
DEFAULT_TEMPERATURE=0.7

# Температура для кода (меньше = точнее)
CODE_TEMPERATURE=0.3

# Максимальная длина ответа (токены)
MAX_NEW_TOKENS=1024

# Максимальный размер файла для анализа (токены)
MAX_FILE_TOKENS=1500

# ---------------------------------------------------------------
# НАСТРОЙКИ RAG (ПОИСК)
# ---------------------------------------------------------------
# Количество результатов поиска
SEARCH_K=5

# Порог схожести (0.0-1.0)
SIMILARITY_THRESHOLD=0.75

# ---------------------------------------------------------------
# НАСТРОЙКИ ОБУЧЕНИЯ (LoRA)
# ---------------------------------------------------------------
# Количество эпох обучения
NUM_TRAIN_EPOCHS=3

# Ранг LoRA (16 для 8GB, 32 для 12GB+)
LORA_R=16

# Скорость обучения
LEARNING_RATE=2e-5

# ---------------------------------------------------------------
# IDE ИНТЕГРАЦИЯ
# ---------------------------------------------------------------
# Максимальная глубина сканирования проекта
MAX_SCAN_DEPTH=5

# Файлы и директории для игнорирования (через запятую)
IGNORE_PATTERNS=*.class,*.o,*.so,*.dll,*.exe,*.log,*.tmp,*.temp,*.cache,.git,.svn,.idea,.vscode,node_modules,dist,build,target,out,.gradle

# ---------------------------------------------------------------
# РАЗРАБОТКА / ДЕБАГ
# ---------------------------------------------------------------
# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Включить детальное логирование обучения
TRAINING_VERBOSE=true

# Сохранять промежуточные чекпоинты
SAVE_CHECKPOINTS=true

# ---------------------------------------------------------------
# API НАСТРОЙКИ (ЕСЛИ ИСПОЛЬЗУЕТЕ API РЕЖИМ)
# ---------------------------------------------------------------
# Порт для API (опционально)
API_PORT=8000

# Хост для API
API_HOST=0.0.0.0

# CORS origins (через запятую)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# ---------------------------------------------------------------
# ПРОИЗВОДИТЕЛЬНОСТЬ
# ---------------------------------------------------------------
# Батч сайз для обработки документов (меньше = медленнее, но стабильнее)
DOCUMENT_BATCH_SIZE=10

# Задержка между запросами (мс) - для предотвращения перегрузки GPU
REQUEST_DELAY_MS=100

# ---------------------------------------------------------------
# БЕЗОПАСНОСТЬ
# ---------------------------------------------------------------
# Максимальный размер файла (MB)
MAX_FILE_SIZE_MB=50

# Разрешённые расширения файлов (через запятую)
ALLOWED_EXTENSIONS=.py,.java,.kt,.js,.ts,.html,.css,.xml,.json,.md,.txt,.pdf,.docx

# Включить проверку типов файлов
ENABLE_FILE_VALIDATION=true

🎯 ЧТО ДЕЛАЕТ
Основные возможности:
📚 Дообучение на документах (PDF/DOCX/TXT/HTML)
💬 Чат с контекстом (RAG - Retrieval Augmented Generation)
🔍 Анализ кода (Python/Java/Kotlin/JavaScript/TypeScript)
🐛 Отладка ошибок (по стеку ошибок)
✅ Генерация кода (по описанию)
🎯 Интеграция с IDE (VSCode, Android Studio)
Специфично для разработки:
Анализирует проекты (структура, зависимости, архитектуру)
Помогает с Android (ViewModel, Compose, Gradle)
Проверяет код (ошибки, улучшения, best practices)
Объясняет логику (по коду из файлов)
💻 СИСТЕМНЫЕ ТРЕБОВАНИЯ
Обязательные:
GPU: NVIDIA P104-100 8GB (или любая 8GB+ GPU)
CUDA: 11.8 или выше
ОС: Kali Linux 2025 (или Ubuntu 20.04+)
RAM: 16GB (минимум 8GB свободно)
Storage: 50GB+ свободного места
Проверьте перед установкой:
nvidia-smi
# Должно показать: GPU Name = P104-100, CUDA Version >= 11.8

cat /etc/os-release | grep PRETTY_NAME
# Должно показать: Kali GNU/Linux 2025

🔧 УСТАНОВКА
ШАГ 1: Подготовка системы
# Обновите Kali
sudo apt update && sudo apt full-upgrade -y

# Создайте рабочую директорию
mkdir -p ~/ai-projects
cd ~/ai-projects

ШАГ 2: Скачивание
# Клонируйте репозиторий
git clone <repository-url> ai-assistant-p104
cd ai-assistant-p104

# Сделайте скрипты исполняемыми
chmod +x scripts/*.sh

ШАГ 3: Автоматическая установка
# Запустите установку (НЕ от root!)
./scripts/setup.sh

Что сделает скрипт:
✅ Проверит GPU P104-100 и CUDA 11.8+
✅ Установит Docker и NVIDIA Container Toolkit (новый метод подписи)
✅ Настроит Python 3.11 и PyTorch для sm_61
✅ Создаст структуру директорий
✅ Настроит переменные окружения
✅ Проверит GPU в Docker
ШАГ 4: Перелогиньтесь (КРИТИЧНО!)
# Примените группу docker
newgrp docker

# ИЛИ просто перелогиньтесь в систему

🚀 ЗАПУСК
Быстрый запуск:
# Запуск через Docker (РЕКОМЕНДУЕТСЯ)
./scripts/run.sh start

# Подождите 2-3 минуты (первый запуск медленный)
./scripts/run.sh cli

Другие команды:
# Статус системы
./scripts/run.sh status

# Просмотр логов
./scripts/run.sh logs

# Остановка
./scripts/run.sh stop

# Перезапуск
./scripts/run.sh restart

Альтернатива (локально):
source venv/bin/activate
python -m src.cli

📖 ИСПОЛЬЗОВАНИЕ
Первый запуск в CLI:

# Добавьте документ
/add /app/data/documents/manual.pdf

# Добавьте проект
/project /workspace/my-android-app

# Проверьте статус
/status

# Задайте вопрос
"Що робить цей метод в моєму проекті?"

# Анализ кода
/code /workspace/my-app/src/main.py

Основные команды CLI:
| Команда           | Описание          | Пример                         |
| ----------------- | ----------------- | ------------------------------ |
| `/help`           | Показать справку  | `/help`                        |
| `/status`         | Статус системы    | `/status`                      |
| `/add <файл>`     | Добавить документ | `/add /app/data/docs/file.pdf` |
| `/project <путь>` | Добавить проект   | `/project /workspace/myapp`    |
| `/train`          | Дообучить модель  | `/train`                       |
| `/clear`          | Очистить экран    | `/clear`                       |
| `/code <файл>`    | Анализ кода       | `/code /app/src/main.py`       |
| `exit/quit`       | Выйти             | `exit`                         |

Примеры запросов:
Для документации:
"Поясни розділ 3 з мого PDF документа"
"Знайди інформацію про помилку 404"
"Як налаштувати сервер?"

Для кода:
"Знайди помилки в цьому коді"
"Як оптимізувати цей метод?"
"Напиши unit тести"
"Поясни алгоритм сортування"

Для Android:
"Як правильно використовувати ViewModel?"
"Знайди помилку в моєму Fragment"
"Напиши Retrofit інтерфейс"

🔌 ИНТЕГРАЦИЯ С IDE
VSCode интеграция:
Вариант 1: Через Tasks
Создайте .vscode/tasks.json:
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "AI: Анализ кода",
      "type": "shell",
      "command": "./scripts/run.sh",
      "args": ["code", "${file}"],
      "problemMatcher": []
    }
  ]
}

Вариант 2: Через Extension API
// В вашем VSCode extension
const axios = require('axios');

async function analyzeCode(code) {
    const response = await axios.post('http://localhost:8000/analyze', {
        code: code,
        language: 'python'
    });
    return response.data;
}

Android Studio интеграция:
Через External Tools:
Name: AI Code Analysis
Program: ./scripts/run.sh
Arguments: code $FilePath$
Working directory: $ProjectFileDir$

Через File Watcher:
# Автоматически анализирует при сохранении
./scripts/run.sh code /path/to/saved/file

Использование в IDE:
1.Анализ текущего файла:
./scripts/run.sh code /workspace/project/src/main.py

2.Добавление всего проекта:
./scripts/run.sh project /workspace/my-android-app

3.Быстрая проверка кода:
В CLI: /code /app/src/main.py

⚙️ НАСТРОЙКА
Основные параметры (.env):
# GPU настройки
CUDA_VISIBLE_DEVICES=0
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:128,garbage_collection_threshold:0.6

# Качество генерации
DEFAULT_TEMPERATURE=0.7        # 0.3 для кода, 0.7 для текста
MAX_NEW_TOKENS=1024           # Максимум 1024 для 8GB

# Поиск
SEARCH_K=5                    # Количество результатов
SIMILARITY_THRESHOLD=0.75     # Порог схожести

Настройка под ваши задачи:
Для кода (точность):
# В config.py
code_temperature = 0.3
max_new_tokens = 512
search_k = 3

Для документации (креативность):
# В config.py  
temperature = 0.7
max_new_tokens = 1024
search_k = 7

Для больших проектов:
# В .env
MAX_FILE_TOKENS=2000
DOCUMENT_BATCH_SIZE=5
MAX_SCAN_DEPTH=7

🛠️ РАЗРАБОТКА
Структура проекта:
ai-assistant/
├── src/                      # Исходный код
│   ├── config.py            # Конфигурация
│   ├── agent.py             # Главный агент
│   ├── document_processor.py # Обработка документов
│   ├── model_trainer.py     # LoRA дообучение
│   ├── rag_engine.py        # RAG система
│   ├── vscode_integration.py # VSCode интеграция
│   └── android_studio_integration.py # Android Studio
├── scripts/                 # Скрипты управления
├── docker/                  # Docker конфигурация
├── data/                    # Данные (монтируется)
└── requirements.txt         # Зависимости

Добавление нового функционала:
Пример: Новый тип документов
# В document_processor.py
elif ext == '.your_format':
    loader = YourFormatLoader(str(path))

Пример: Новая команда CLI
# В cli.py
elif command == '/your_command':
    self._handle_your_command(args)

Отладка:
Проверка VRAM:
# В CLI
/status

# Или вне контейнера
nvidia-smi

Логи обучения:
./scripts/run.sh logs
# Ищите строки с "training_loss"

Детальное логирование:
# Включите DEBUG в .env
LOG_LEVEL=DEBUG

📊 ПРОИЗВОДИТЕЛЬНОСТЬ
На P104-100 8GB VRAM:
Table
Copy
Задача	Время	VRAM	Примечание
Загрузка модели	30-60 сек	6.8GB	Первый запуск
Генерация (100 токенов)	12-15 сек	7.2GB	Поиск + генерация
Анализ файла (200 строк)	20-25 сек	7.3GB	RAG + анализ
Дообучение (1 эпоха, 100 файлов)	45-60 мин	7.5GB	LoRA fine-tuning
Оптимизации для скорости:
Уменьшите max_new_tokens до 512
Уменьшите search_k до 3
Используйте code_temperature=0.3
Очищайте кэш регулярно через /status
🐛 РЕШЕНИЕ ПРОБЛЕМ
❌ CUDA out of memory
# Проверьте 8-bit квантизацию
grep "load_in_8bit" src/config.py
# Должно быть True

# Очистите память
./scripts/run.sh cli
# Введите exit для очистки

❌ Docker cannot connect to daemon
# Перелогиньтесь
newgrp docker

# Или перезапустите
sudo systemctl restart docker

❌ Repository is not signed (Kali 2025)
# Скрипт уже использует новый метод!
# Если ошибка:
sudo rm -f /usr/share/keyrings/*docker*
./scripts/setup.sh

❌ Permission denied
# Исправьте права
sudo chown -R $USER:$USER data/
chmod -R 755 data/

❌ GPU not detected in Docker
# Проверьте nvidia-container-toolkit
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker


📁 СТРУКТУРА ДАННЫХ
data/
├── documents/          # Ваши PDF/DOCX/TXT/HTML файлы
│   ├── manual.pdf
│   ├── api-docs.docx
│   └── readme.txt
├── models/            # Кэш моделей и LoRA адаптеры
│   ├── mistral-7b-instruct-v0.3/  # Базовая модель
│   └── lora_20240101_120000/      # Дообученная модель
├── chroma/            # Векторная база данных
│   └── chroma.sqlite3
├── cache/             # Временные файлы
└── vscode_projects/   # Кэш проектов IDE

📞 ПОДДЕРЖКА
Если не работает:
Проверьте системные требования
Запустите ./scripts/setup.sh еще раз
Проверьте логи: ./scripts/run.sh logs
Убедитесь что nvidia-smi показывает P104-100
Где спросить:
Создайте Issue в репозитории
Укажите вывод nvidia-smi и ./scripts/run.sh status

✅ ПРОЕКТ ГОТОВ К ИСПОЛЬЗОВАНИЮ!
Начните с ./scripts/run.sh start и ./scripts/run.sh cli